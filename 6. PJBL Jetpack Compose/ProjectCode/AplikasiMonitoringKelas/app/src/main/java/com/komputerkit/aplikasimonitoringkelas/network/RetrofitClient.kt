package com.komputerkit.aplikasimonitoringkelas.network

import com.komputerkit.aplikasimonitoringkelas.data.api.ApiConfig

// Placeholder class to satisfy imports until the ViewModel is updated
object RetrofitClient {
    val apiService = ApiConfig.apiService

    fun initialize(context: android.content.Context) {
        // Initialization is handled in ApiConfig
    }
}